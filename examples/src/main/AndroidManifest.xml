<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Orchestra">
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.FadeActivity"
            android:exported="true"
            android:label="@string/example_fade" />
        <activity
            android:name=".activities.SlideActivity"
            android:exported="true"
            android:label="@string/example_slide" />
        <activity
            android:name=".activities.TranslateActivity"
            android:exported="true"
            android:label="@string/example_translate" />
        <activity
            android:name=".activities.SpacingActivity"
            android:exported="true"
            android:label="@string/example_spacing" />
        <activity
            android:name=".activities.ScalingActivity"
            android:exported="true"
            android:label="@string/example_scaling" />
    </application>

</manifest>
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elihimas.orchestra">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar.Full">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.FadeExampleActivity"
            android:label="@string/example_fade" />
        <activity
            android:name=".activities.ScaleExampleActivity"
            android:label="@string/example_scale" />
        <activity
            android:name=".activities.InterpolatorActivity"
            android:label="@string/example_interpolator" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/examples_title" />
        <activity
            android:name=".ui.animateimage.AnimateImageActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.BackgroundAndTextColorExampleActivity"
            android:label="@string/example_background_text_color" />
        <activity
            android:name=".activities.BouncingButtonExampleActivity"
            android:label="@string/example_bouncing" />
        <activity
            android:name=".activities.ConstrainsExampleActivity"
            android:label="@string/example_constrains_layout" />
        <activity
            android:name=".activities.CoordinatorExampleActivity"
            android:label="@string/example_coordinator_layout" />
        <activity
            android:name=".activities.SlideExampleActivity"
            android:label="@string/example_slide"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ExtensionsExamplesActivity"
            android:label="@string/example_extensions" />
    </application>

</manifest>
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elihimas.orchestra">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.CoordinatorExampleActivity"></activity>
        <activity android:name=".activities.ScaleExampleActivity" />
        <activity android:name=".activities.TranslateExampleActivity" />
        <activity android:name=".activities.FadeOutExampleActivity" />
        <activity android:name=".activities.FadeInExampleActivity" />
        <activity android:name=".activities.FormExampleActivity" />
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/examples_title" />
    </application>

</manifest>